# Knowledge — Decision Log

Architectural decisions with rationale. Grouped by domain; within each decision table, rows are sorted by decision ID. Decision IDs are globally unique through **D132** (V1 Greenfield implementation rows **D95–D102** were renumbered from a duplicate D81–D88 block that collided with Strategic Direction **D81–D88**). Decision IDs are append-only — never reused. Resolved issues use R-prefixed IDs.

---

## Active Issues

| ID | Issue | Status |
|----|-------|--------|
| D14 | Schwab `TRADE → BUY` mapping loses sell-side distinction | Open |
| D15 | TastyTrade tax lots are synthetic (CALCULATED) — not for tax reporting | Accepted limitation |
| D56 | Copilot reviewer cannot be auto-added | Repo admin must invite GitHub Copilot as a collaborator (or upgrade to a plan that ships the `@copilot` reviewer slug). Workflow `request-copilot-review.yml` runs but silently no-ops because `copilot-pull-request-reviewer` is not a collaborator on `sankalp404/axiomfolio` (HTTP 422 on `requested_reviewers`). |

---

## Architecture Decisions

### Stage Analysis & Regime

| ID | Date | Decision |
|----|------|----------|
| D1 | 2026-03-23 | **SMA150 replaces 30-week SMA** as primary stage anchor (per Stage_Analysis.docx) |
| D2 | 2026-03-23 | **Regime Engine is mandatory hard gate** — R1-R5 gates all sizing, scan access, exits |
| D3 | 2026-03-23 | **10 sub-stages** (1A/1B, 2A/2B/2C, 3A/3B, 4A/4B/4C) replace simplified labels |
| D9 | 2026-03-23 | **Stage_Analysis.docx is canonical spec** — all implementation must match |
| D10 | 2026-03-23 | **SMA150 slope uses 20-day lookback**, thresholds ±0.35% |
| D11 | 2026-03-23 | **EMA10 Distance normalized by ATR** (`EMA10_Dist_N = EMA10_Dist% / ATR%14`) |
| D46 | 2026-03-31 | **ATR/ADX use Wilder's smoothing** — SMA seed for first N bars, then recursive `(prev * (N-1) + curr) / N`. Not simple rolling mean. |
| D47 | 2026-03-31 | **52-week High/Low from H/L columns** — not Close. Fallback to Close only if H/L missing. |
| D48 | 2026-03-31 | **Bollinger Bands use population std** (ddof=0) — not sample std (ddof=1). |
| D49 | 2026-03-31 | **TD Sequential caps at 9** — counter resets to 0 after completing a 9-count setup. |
| D50 | 2026-03-31 | **Bar guard = 175** — minimum bars needed for reliable stage classification (SMA150 + 20-bar slope + 5-bar warmup). |
| D51 | 2026-03-31 | **UNKNOWN stage treated as null** — `compute_stage_run_lengths` and `repair_stage_history_window` skip UNKNOWN labels. Displayed as "New" in UI. |
| D52 | 2026-03-31 | **Regime rounding uses half-up** — `math.floor(avg * 2 + 0.5) / 2` not Python's banker's `round()`. |
| D53 | 2026-03-31 | **RiskGate.check mandatory on submit path** — not just preview. Rejected orders logged with reason. |
| D54 | 2026-04-04 | **FMP rate limits via ProviderPolicy** — single `MARKET_PROVIDER_POLICY` env var selects tier (free/starter/paid/unlimited). Legacy per-var config (`RATE_LIMIT_FMP_CPM`, `PROVIDER_DAILY_BUDGET_FMP`) deprecated. |
| D55 | 2026-04-04 | **Deep backfill bypasses L2 DB cache** — `daily_since` sets `skip_l2=True` so `get_historical_data` always reaches L3 external APIs. Prevents partial-history short-circuit when DB already has fresh bars. |
| D59 | 2026-04-05 | **VolatilityService extracted** — VIX/VVIX/VIX3M logic moved from inline route handler to `backend/services/market/volatility_service.py`. Route is now a thin wrapper. |
| D60 | 2026-04-05 | **Dashboard universe filter** — `GET /dashboard?universe=all\|etf\|holdings` filters tracked symbols server-side before all aggregations. Cache key includes universe. |
| D61 | 2026-04-05 | **Server-side table endpoint** — `GET /snapshots/table` with sort/page/filter replaces client-side 5000-row fetch for the scanner/table views. |
| D62 | 2026-04-05 | **Frontend dedup hooks** — `useVolatility()` and `useRegime()` share React Query cache across TopDownView/RegimeBanner/MarketDashboard. |
| D63 | 2026-04-05 | **Budget check fail-closed** — if Redis is unreachable during FMP daily budget check, the provider is skipped (fail-closed) instead of allowing unlimited calls. |

### Execution & Orders

| ID | Date | Decision |
|----|------|----------|
| D6 | 2026-03-23 | **Single order path**: OrderManager → RiskGate → BrokerRouter (OrderService merged) |
| D16 | 2026-03-23 | **No alternative execution paths** — all orders through OrderManager |
| D18 | 2026-03-23 | **IBKR is primary execution broker** — Schwab/TastyTrade read-only for now |

### Frontend & UX

| ID | Date | Decision |
|----|------|----------|
| D13 | 2026-03-23 | **Bloomberg-style multi-view dashboard**: Top-Down, Bottom-Up, Sector, Heatmap |
| D22 | 2026-03-24 | **Intelligence brief has Universe + Book sections** |
| D23 | 2026-03-24 | **TopDown is dual-mode** — Universe view + Portfolio view |
| D108 | 2026-04-19 | **Command-K palette with action registry** — Cmd/Ctrl+K opens a `cmdk`-based palette (~6 KB) for navigation, ticker jumps, settings, and recent actions. Features register actions via `actionRegistry` and receive an unregister fn so per-page surfaces mount/unmount cleanly. Recent actions persist top-5 to localStorage; binding uses capture phase so it coexists with the existing chat hotkey. `?` opens a keyboard shortcut overlay. PR #348. |
| D109 | 2026-04-18 | **Design foundation: Geist + 8-series palette + ChartGlassCard** — Geist Sans/Mono self-hosted via `@fontsource-variable` (Inter retained as fallback), with `cv11/ss01/ss03` enabled and a `.tabular-nums` utility for monetary alignment. Eight-series chart palette exposed as `--series-1`..`--series-8` CSS variables tuned per theme, with an Okabe-Ito color-blind variant gated by `[data-palette="cb"]` and persisted via `useUserPreferences()`. `ChartGlassCard` is the canonical chart frame (resting/hover/active/floating elevation, glass + interactive variants, dark-mode inset highlight). `prefers-reduced-motion` respected globally at `@layer base`. PR #349. |
| D125 | 2026-04-21 | **Portfolio-first IA with Home + Signals and Cmd+K replacing Terminal.** Navigation is re-cut to Home / Portfolio / Signals / Market / Lab / Learn / Settings, removing Terminal from sidebar and moving power-user jumps into a global command palette (`Cmd/Ctrl+K`). Route migrations keep deep links alive (`/picks` -> `/signals/picks`, `/portfolio/workspace` -> `/market/workspace`, `/portfolio/allocation` -> `/portfolio?tab=allocation`), while Categories stays routable but hidden from primary nav. Home becomes the personal landing surface (day P&L, attention preview, peak-signal preview, picks lock-preview, AI narrative, market pulse) and shows explicit loading/error/empty/data states to avoid silent fallback masking. |

### Strategic Direction

Master-plan decisions **D81–D90** merged from `feat/v1-master-plan` (PR #316, 2026-04-18). **D91–D94** extend that base in subsequent v1-sprint PRs.

| ID | Date | Decision |
|----|------|----------|
| D81 | 2026-04-09 | **Two-milestone plan replaces 6+ month roadmap** — v1 Launch 2026-06-21 (10 weeks) ships paying users on 6 tiers + Validated Picks + native AgentBrain chat + Snowball viz + TRIM/ADD/rebalance + tax-aware exits + email parser + PWA mobile. World-Class Complete 2026-08-31 (20 weeks) ships Plaid + 5 brokers + walk-forward + Monte Carlo + symbol master + multi-source quorum + OpenTelemetry + chaos + Trade Copy + AI optimizer. No features cut, just sequenced. Master plan: `docs/plans/MASTER_PLAN_2026.md`. Reversible: revert plan + reduce milestone scope. |
| D82 | 2026-04-09 | **Six-tier subscription model** — Free / Lite / Pro / Pro+ / Quant Desk / Enterprise. Free = read-only Snowball viz + 24h-delayed picks (acquisition hook). Lite = real-time picks (no autotrade). Pro = autotrade + tax-aware exits. Pro+ = + native AgentBrain chat + multi-portfolio + advanced backtests. Quant Desk = + walk-forward + plugin SDK + white-label. Enterprise = SSO + SLA + custom integrations. Stripe-backed; `User.tier` enum + `Entitlement` table. Alternatives: 3-tier (rejected — less ARR per user), usage-based (rejected — fights retail intuition). Reversible. |
| D83 | 2026-04-09 | **Native AgentBrain chat panel as Pro+ hook** — Pro+ users see pinned right-rail chat panel powered by internal `AgentBrain`. Auto-injects portfolio context, current page, recent picks. Differentiates from Snowball (no chat) and StocksToBuyNow (signals only, no analysis). Cross-sell to Paperwork Brain for cross-domain conversational AI remains optional. Alternative: route everything through external Paperwork Brain (rejected — couples products, weakens AxiomFolio standalone value); ship to Pro instead of Pro+ (rejected — anchor feature for Pro+ upgrade). Reversible: gate behind feature flag. |
| D84 | 2026-04-09 | **Twisted Slice pseudonym for hedge fund validator** — All references to founder's brother in code, docs, UI, marketing copy, public scorecard use `twisted_slice` / "Twisted Slice". Real identifying details live only in the `PICKS_TRUSTED_SENDERS` env var (server-side, never exposed in code, docs, version control, or any user-facing artifact). Enforced via `.cursor/rules/validator-curator.mdc`. Alternatives: real name (rejected — privacy), generic "validator" (rejected — less brand). Reversible. |
| D85 | 2026-04-09 | **Polymorphic LLM picks parser, not template-based** — Inbound emails from validators have N different formats (single-stock bullets, hedge fund research recap with timestamps, research-note paragraph, daily market recap narrative, mixed). LLM (gpt-4o for synthesis, gpt-4o-mini for triage) picks the right schema template at parse time and extracts polymorphically. Schema set: `ValidatedPick`, `MacroOutlook`, `SectorRanking`, `PositionChange`, `RegimeCall`, `FlowSignal`, `AttributedQuote`. Cross-email signal linking via vector similarity + symbolic level matching. Alternatives: per-template regex parsers (rejected — fragile, doesn't generalize), pure structured-output API (rejected — emails are too varied). Reversible: per-format fallback parsers. |
| D86 | 2026-04-09 | **PDF + image attachment ingestion first-class** — Validators forward research notes as PDFs with embedded charts (e.g., ZeroHedge daily). `pymupdf` for PDF text + image extraction, gpt-4o vision for chart captioning. Cost-gated to trusted senders (vision is expensive). Email + PDF dedupe layer flags duplicates and merges into single ValidatedPick draft. Alternative: text-only ingestion (rejected — drops half the signal). Reversible: disable vision via flag. |
| D87 | 2026-04-09 | **Per-tier LLM cost budgets** — Free: $0/mo, Lite: $0/mo, Pro: $5/mo, Pro+: $20/mo, Quant: unlimited. Tracked via Langfuse per-user per-feature. Hard cap with degradation to "feature unavailable this month" message. Alternatives: per-call charging (rejected — fights retail UX), no cap (rejected — runaway costs). Reversible. |
| D88 | 2026-04-09 | **Multi-tenancy hardening blocks v1 launch** — Current code has hardcoded `BRAIN_TOOLS_USER_ID=1` and global `CircuitBreaker`. Per-user circuit breaker, per-tenant Redis namespaces, per-tenant rate limits, GDPR data export/delete are v1 launch requirements. Tracked in MASTER_PLAN_2026 Phase 8d (lifted from World-Class to v1 critical path). Alternative: ship single-tenant first (rejected — billing requires multi-tenant). Reversible: revert per-user changes. |
| D89 | 2026-04-09 | **FileFree and AxiomFolio integrate via Brain, never direct imports** — FileFree ([filefree.ai](https://filefree.ai/)) is a separate Paperwork Labs product. AxiomFolio exports year-end tax package (1099 + tax lots + wash-sale ledger) via existing brain webhook channel. FileFree consumes via Brain. Codebases stay independent. Alternative: shared monorepo (rejected — couples release cadence). Reversible: collapse via direct API if needed. |
| D90 | 2026-04-09 | **Cloud Background Agents pattern for parallel sprint execution** — Heavy v1+WC sprint dispatched as parallel Cursor Background Agents from this Opus orchestrator. Each agent owns one PR end-to-end (clone, branch, code, test, push, open PR, fix CI). Founder reviews/merges from anywhere (laptop or Cursor mobile). Opus stays in-session for architecture, schema, code review of agent output, decision logging. Pattern documented in `.cursor/rules/delegation.mdc`. Alternative: serial in-session work (rejected — too slow for 10-week v1 timeline). Reversible. |
| D91 | 2026-04-18 | **Entitlement is the single source of truth for tier access.** `backend/services/billing/feature_catalog.py` is the only place that maps a feature key to a minimum tier. Backend (`require_feature` dependency) and frontend (`useEntitlement` + `<TierGate>`) both consume it so they cannot drift. Stripe webhooks are the only writers besides operator manual overrides. Past-due subscriptions get a 72h grace period; manual entitlements (founders, validator pseudonyms) cannot be overwritten by Stripe events. |
| D92 | 2026-04-18 | **Tier-gating returns HTTP 402 (Payment Required), not 403.** This lets the frontend distinguish "you need to log in" from "you need to upgrade" without a string sniff on the error body, matching how Stripe-gated APIs across the industry signal paywalls. Routes mounted via `Depends(require_feature("..."))` always return a structured `{error: "tier_required", feature, current_tier, required_tier}` body. |
| D93 | 2026-04-18 | **CI signals are forwarded into the test container.** `infra/compose.test.yaml` now passes `CI`, `GITHUB_ACTIONS`, and `GITHUB_RUN_ID` from the GitHub Actions runner into `backend_test`. `_in_ci()` reads any of the three (`CI=false` explicitly opts out only when no other signal is set). Without forwarding, the runner saw `CI=true` but pytest inside the container did not, so the schema guard's hard-fail branch never fired and ~500 DB tests skipped silently while CI reported green. The same hole hid the PR #321 `op.autocommit_block()` migration regression for ~8 days. Regression tests in `backend/tests/test_ci_hard_fail_on_missing_db.py` lock in detection for each env var. |
| D94 | 2026-04-18 | **AutoOps explanations are persisted, rate-limited, and never on the critical path.** `AnomalyExplainer` output is stored in `auto_ops_explanations` (one row per `(anomaly_id, generated_at)`; full payload as JSON, hot fields denormalized for list queries). The `auto_remediate_health` task fans out a `backend.tasks.ops.explain_anomaly` task per non-green dimension via `send_task` so a slow LLM never blocks remediation. `recent_explanation_within` collapses flapping dimensions to one LLM call per cadence window. Operators consume explanations via `GET /api/v1/admin/agent/explanations` (paginated audit) and `POST /api/v1/admin/agent/explain[/dimension]` (synchronous "explain now"). Failures degrade to the deterministic fallback explainer; the schema's `is_fallback` flag tells the UI to render a degraded badge so trust stays calibrated. |
| D104 | 2026-04-18 | **KNOWLEDGE.md ID re-numbering protocol enforced.** V1 Greenfield rows that had been logged as a duplicate D81–D88 block (colliding with Strategic Direction D81–D88) were renumbered to D95–D102, with a permanent cross-walk note in this file. Strategic Direction tables now include D91–D94 inline; Infrastructure & Ops and Data Pipeline tables sorted by ID; the Copilot active-issue row renumbered to D56 so D89 is unique. Decision IDs are append-only from this point forward — never reused. PR #344. |
| D107 | 2026-04-19 | **Calibrated transparency over internal cost disclosure.** `/why-free` is a public scrollytelling page anchored on industry economics (Plaid math, SnapTrade pricing, OpenAI token costs) and solo-founder authenticity, not a Buffer-style internal cost dashboard. Public `GET /api/v1/public/stats` (5-minute cache) feeds a small stats strip; tip-jar buttons render disabled until Stripe Checkout is wired. Internal cost / `/metrics` disclosure is deferred to v1.x once revenue is meaningful enough to share. PR #347. |
| D113 | 2026-04-20 | **AxiomFolio's product thesis is the discipline layer, not the picker layer.** Validated through a live founder workflow today: external momentum sources (StocksToBuyNow.ai, IBD-style screeners, social signals) are good at *entries* — they surface stocks that are moving — but silent on *exits*. The classic failure mode is asymmetric: winners get realized (user happy to sell at +50%), losers get held ("the algorithm liked it; it'll come back"). Realized P&L looks great; unrealized losses pile up silently as dead capital. Founder lived this exact pattern with StocksToBuyNow.ai over the past year (own admission). AxiomFolio's differentiation is therefore not "better picks", but "**the discipline layer that picker services don't give you**" — turning *I made money but also lost money* into *I systematically captured edge with measured, mechanically-enforced drawdown*. This re-prioritizes the entire roadmap: **G14 Position Health Auditor (stuck-loss detection + tax harvest overlay) is now P0**, because without it AxiomFolio is interchangeable with any other picker and has no defensible moat. Picker quality (G7 PickQualityScorer) remains important but is now a supporting feature, not the headline. Concretely: every paying-tier pricing page must lead with "we tell you when to *exit*", every Trade Card (G10) must render exit-side discipline as visibly as entry-side scoring, and the "Position Health" tile becomes a permanent first-class dashboard surface. Alternatives rejected: (a) lead with "better picks than StocksToBuyNow.ai" (rejected — head-to-head momentum-screener bake-off is unwinnable and misses the actual user pain); (b) ship picker-first and add discipline in v1.5 (rejected — the moat is the discipline, shipping picker-only first builds the wrong customer expectation that's hard to reverse). Reversible: re-prioritize gap order if discovery proves picker quality is the real bottleneck. Companion: see `docs/plans/GAPS_2026Q2.md` for full G1–G14 gap inventory anchored on this thesis. |
| D114 | 2026-04-20 | **Trade-ledger state is first-class data, not derived UI.** Surfaced by a live position-screen reading today: Schwab (and most brokers) display only `current_quantity` + `cost_basis_remaining` + `unrealized_pnl`, which is the post-scale aggregate. The same numbers describe radically different positions: a fresh +208% MSTR call (full risk applies, manage tightly into earnings) vs a runner +208% MSTR call where the bulk was already scaled out Friday and the remainder is house money (asymmetric ride, GTC moonshot limits are correct, earnings risk is acceptable). System advice flips opposite based on which it is. Without a trade-ledger model, AxiomFolio's alerts will give the wrong recommendation half the time and erode user trust. **Architectural commitment**: every broker sync (IBKR FlexQuery, TastyTrade SDK, Schwab) must ingest the **full trade history per contract** (not just current snapshot) into a new `OptionContractTrade` table; a `RunnerStateClassifier` runs after every sync and stamps each open position with `runner_state ∈ {fresh, partial_scale, runner, pyramid}`; Trade Card (G10), PickQualityScorer (G7), Position Health Auditor (G14), and every alert/digest read this state and adjust their language and recommended actions accordingly. The same ledger feeds (a) tax-loss harvesting (need lot-level history), (b) backtest fidelity (don't pollute candidate-generator win rates with runner P&L), and (c) the "stuck position" classification in G14 (held > N days, repeated drawdown, no thesis). Alternatives rejected: (a) infer runner-state from current cost-basis heuristics alone (rejected — ambiguous; user could have multiple legs at same cost basis from independent entries); (b) ask the user to manually tag positions (rejected — defeats the purpose of automation, and users won't do it consistently). Reversible: trade history is additive; if the classifier proves noisy we can fall back to "fresh assumed unless user tags otherwise" without losing data. Companion: G13 (runner-state) + G14 (position health) in `docs/plans/GAPS_2026Q2.md`. |
| D115 | 2026-04-20 | **External signal aggregators are score inputs with measured forward-return weights, never auto-trade overrides.** Surfaced today by user receiving a BUY signal on QBTS from StocksToBuyNow.ai while AxiomFolio's snapshot rated QBTS `action_label=AVOID` (Stage 4A, below SMA150/SMA200, TD Sell 7, earnings 24d). Both verdicts are internally consistent within their own methodology — momentum screener vs Stage discipline are different philosophies with different track records, and both have valid practitioners. The right architecture is to **ingest external signals**, **measure each provider's forward-return Sharpe over rolling windows**, and **weight them in the PickQualityScorer (G7)** by their measured edge, not by reputation. Provider abstraction `ExternalSignalProvider` with adapters per source (StocksToBuyNow.ai first, then Zacks/IBD/Finviz/TipRanks); signals stored in `external_signal` table with `forward_30d_return` backfilled by a 30-day-later Beat job; provider quality summarized in `external_provider_quality` with rolling Sharpe. **Hard rule (codified, not commented)**: external signals NEVER bypass risk_gate, regime_gate, or earnings blackout — they affect the *score* of a candidate, never the *go/no-go*. **Trade Card (G10) renders external alignment row** (`System: BUY (ok) · StocksToBuyNow.ai: BUY (ok) · Zacks: HOLD`) and **renders a prominent `(warning) Methodology disagreement` badge** when system and external consensus diverge, linking to a "why" expansion that lists each signal's own track record. **Anti-goal**: never let this become "follow the gurus" — a provider that goes cold gets demoted automatically when its 90-day rolling Sharpe drops below 0; user-trusted providers with no measured edge get weight 0. Alternatives rejected: (a) treat external signals as veto / auto-buy (rejected — that's StocksToBuyNow.ai with extra steps); (b) reject all external signals as noise (rejected — provider track records vary, some have real edge, the system should learn which); (c) hard-code provider weights based on subjective trust (rejected — measurement, not opinion, is what makes this defensible). Reversible: per-provider enable flag plus the override `PickQualityScorer.external_signal_weight=0` removes the input cleanly. Companion: G12 in `docs/plans/GAPS_2026Q2.md`. |
| D116 | 2026-04-20 | **AxiomFolio is a dual-mode return-maximization platform — extends D113.** D113 framed AxiomFolio as the discipline layer that prevents unrealized losses. Today's session surfaced the symmetric other half: the same trader who silently accumulates losers also silently leaves gains on the table by failing to scale at peaks. The founder's own IBKR account peaked at $1.85M on 2025-10-31, would have been ≥$2.4M with disciplined peak-scaling, and is currently $1.4M — a $450k unrealized-gain evaporation that mirrors the unrealized-loss accumulation problem and has the same root cause (no system-surfaced signal at the moment of decision, plus tax-deferral psychology). The product thesis therefore expands to: **AxiomFolio is the dual-mode return-maximization platform that (1) cuts losers via Position Health Auditor (G14), (2) scales winners at peaks via Peak/Profit-Taking Signal Engine (G15), (3) eliminates the tax-deferral friction via Tax-Aware Exit Calculator (G16), (4) caps portfolio leverage per market regime via Margin/Leverage Risk Surface (G17), and (5) supports both Active-trading and Conviction-holding modes per position via the Two-Mode Sleeve System (G18) — with a separate Conviction Pick Generator (G19) feeding multi-year buy/hold ideas into the conviction sleeve.** Success metric is **alpha vs SPY at the user's chosen volatility**, not Sharpe-maximization vs SPY (founder's stated preference: "S&P is slow for me say active trader"). The user explicitly chooses higher volatility for higher expected return; the system's job is to manage that responsibly without preaching down the volatility. The anti-goal is becoming "another robo-advisor that recommends boring index funds" — AxiomFolio explicitly serves the trader who wants outsized returns and is willing to accept measured drawdown to get them. Validated against live evidence today: queried prod and confirmed the system already says `action_label=REDUCE` on the user's AMZN/GOOGL/GOOG/META/NFLX positions (96%+ of 52w ATH on the first three) and `AVOID` on MSFT and `SHORT` on ACHR — the verdicts exist, the surface to deliver them to the user does not. Alternatives rejected: (a) ship discipline-only (D113 alone) and add return-maximization in v1.5 (rejected — same trader needs both, half-product is not a product); (b) push toward lower-vol allocation as the recommendation default (rejected — that's not who the user is, and that's not who the target customer is); (c) make Conviction sleeve a separate product (rejected — same user, same dashboard, single-system thesis is the differentiator). Reversible: each gap (G15-G19) is independently shippable behind a feature flag; revert is per-gap. Companion: `docs/plans/GAPS_2026Q2.md` G15-G19 + the G15 worked-example table anchored on the user's actual Schwab holds. |
| D117 | 2026-04-20 | **Broker credentials authenticate at the user level; account discovery must be plural by default.** Surfaced live: founder added IBKR Flex credentials that legitimately cover BOTH `U15891532` (Traditional IRA) and `U19490886` (Joint Taxable with spouse), but AxiomFolio's onboarding silently picked the first account in the report and defaulted `account_type=TAXABLE` regardless of broker-reported type. The broker (IBKR) emits `<AccountInformation>` sections per account with `AccountType` + `CustomerType` + `AccountCapabilities` — every field needed to correctly classify is in the data we already fetch; we just discard it. Architectural commitment: every broker integration must expose a `discover()` method that returns `list[DiscoveredAccount]`, account_type is set from broker-reported metadata never from a UI default, onboarding shows a checklist of all discovered accounts with broker-provided names so the user picks knowingly, and a weekly re-discovery job surfaces newly-opened accounts for opt-in. Same pattern applies to TastyTrade and Schwab — they're just behind on this PR. Reversible only by retypings and re-onboarding (cheap during single-user beta, expensive once we have N users with miscategorized accounts). See G25 + G24. |
| D118 | 2026-04-20 | **Founder portfolio is the canonical replay corpus and acceptance test for G14/G15/G16; engine versions ship only when replay-measured improvement ≥ documented threshold.** Surfaced today: founder explicitly framed their own IBKR/Schwab portfolio as the model AxiomFolio should be built against ("my port can help us see the vision of how max it would have been"). Architecturally this means (1) historical broker import (G23) is a hard prerequisite for the replay harness (G21); (2) every engine in the discipline-layer family (G14 Position Health, G15 Peak Signal, G16 Tax-Aware Exit, eventually ExitCascade tax-aware tier) carries a replay-test in CI that fails the build if a new version regresses real-world counterfactual P&L vs the prior version on the founder corpus; (3) the founder-self-identified failure mode "inability to close when winning" (G26) becomes the explicit pass criterion for G15 — not an abstract "peak signal accuracy" metric — and any G15 PR description must report the four G26 acceptance numbers. Anti-pattern: never use the replay harness to silently tune engine thresholds (overfitting to one trader); thresholds remain spec-driven, the harness only validates. Reversible if we later need to swap the replay corpus to a different (e.g. de-identified user pool) — but the discipline of "ship only with measured replay improvement" is permanent. |
| D119 | 2026-04-20 | **Founder year-end target tiered with leverage explicitly priced in; discipline-bounded anchors are: unleveraged $1.95M–$2.5M, with ~2× leverage (founder has $1.67M margin available, total buying power ~$3.07M) $2.5M–$3.0M, speculative-aggressive (~2.5× leverage) $3.0M–$3.5M, and beyond $3.5M = breaks discipline regardless of leverage.** Founder asserted $3M is reachable using $1.67M margin (2026-04-20); math confirms this is plausible at ~2× sustained leverage with R1/R2 regime + G14 cutting losers + G15 capturing peaks + zero margin calls — equivalent to ~115–215% annualized return on equity, which sits at the top edge of professional outcomes but is not lottery territory the way $4M would be (~330% annualized = lottery). Symmetric downside that must be displayed alongside leverage decisions: a 30% drawdown on a 2× leveraged book = 60% equity loss = $1.4M → $560k. Leverage is amplitude in both directions; same coin. **G17 (Margin / Leverage Risk Surface) is therefore load-bearing, not optional — every leverage-using position must show a live "if-thesis-wrong" counterfactual ($X equity remaining) next to the position size, otherwise the leverage decision is being made blind.** $4M still requires breaking discipline (would need ~3× sustained leverage, which exceeds Reg-T initial margin of 50% and forces Portfolio Margin reclassification, AND zero drawdowns — not plannable). Compounding reference: at 50%/year from $1.4M → $10.6M in 5 years; at 30%/year → $5.2M in 5 years; blowing up at 2× → $560k and a decade of recovery. Anchor for founder-portfolio replay (D118): at year-end, did the system land in $2.5M–$3.0M with leverage usage staying below the discipline-bounded ceiling AND did G14/G15/G17 surface the correct alerts on the founder's actual book? If yes, the discipline-with-leverage thesis is validated for raising/recruiting/pricing the premium tier. If no, iterate on engines (G14/G15/G17), not on the target. The $4M aspiration is acknowledged and not enforced — system will not block the founder from pursuing it manually, but Risk Gate will block any auto-suggested position sizing that would require it. Reversible mid-year if a true regime tailwind appears (revisit then with fresh data, never with hope). |

### Infrastructure & Ops

| ID | Date | Decision |
|----|------|----------|
| D4 | 2026-03-23 | **Persona-based cursor rules** for domain-specific AI context |
| D5 | 2026-03-23 | **Celery time limits per-task** — match job_catalog.py timeout_s |
| D17 | 2026-03-23 | **Intelligence Brief system** — daily/weekly/monthly proactive delivery |
| D25 | 2026-03-24 | **Auto-Ops Agent** — 15-min health checks with auto-remediation |
| D28 | 2026-03-24 | **Greenfield DB rebuild** — single baseline migration, fresh Postgres |
| D29 | 2026-03-25 | **Celery task routing must match `name=`** exactly — no auto-derivation |
| D30 | 2026-03-25 | **Quarterly dependency audit** — Postgres, Python, Vite major versions |
| D31 | 2026-03-26 | **Agent codebase access policy** — allowlist prefixes, denylist sensitive patterns |
| D42 | 2026-03-28 | **IBKR Gateway settings persistence** — `TWS_SETTINGS_PATH` + volume mount |
| D44 | 2026-03-31 | **Celery Beat replaces Render crons** — all scheduling via job_catalog.py, Beat embedded in worker with `--beat` flag. Legacy Render crons suspended; sync code (`render_sync_service.py`) retained if platform crons are re-enabled |
| D45 | 2026-03-31 | **run_task_now in INLINE_ONLY** — MODERATE risk tool must route through _execute_safe_tool, not _dispatch_celery_task (no TOOL_TO_CELERY_TASK mapping needed) |
| D74 | 2026-04-10 | **Statement timeout 30s on all DB connections** — Added `-c statement_timeout=30000` to SQLAlchemy connect_args to prevent runaway queries from blocking connection pool. Configurable via DB_STATEMENT_TIMEOUT_MS env var. Alternatives: per-query timeout (fragile), pg-level idle_in_transaction_session_timeout (complementary). Reversible: remove connect_args. |
| D75 | 2026-04-10 | **Health check split: /health (fast) vs /health/full (DB)** — Render health probes now hit a lightweight /health endpoint with no DB queries. Full DB validation moved to /health/full for admin use. Eliminates health check timeouts that caused 502 cascades. Alternatives: cache health response (still risks first-request timeout). Reversible: merge endpoints back. |
| D76 | 2026-04-16 | **Worker plan: Render Standard (2 GiB)** — Worker on Starter (512 MiB) was OOM-killed daily. 24h memory observed: avg 430 / peak 508.6 MiB on a 512 MiB ceiling = zero headroom. Indicator recompute over 2,500 symbols + IBKR FlexQuery + OrderManager + Beat in one process legitimately needs 1+ GiB. `--max-memory-per-child` raised in lockstep from 750000 (above ceiling, never fired) to 1500000 KiB so Celery recycles before kernel OOM. Alternatives: Render Pro 4 GiB ($85/mo, only if a full snapshot_history rebuild is added back); migrate to DigitalOcean App Platform (Plan B). Reversible: revert render.yaml. |
| D77 | 2026-04-16 | **API uvicorn workers: 2** — PR #295 cut workers from 2 → 1 on 2026-04-06 to mask memory pressure. Real cause (heavy synchronous `build_dashboard()` in web process) was fixed in `291f2e0` on 2026-04-11. API peak RAM observed at 255 MiB / 2 GiB Standard plan = ample room for 2 workers. Restoring doubles request capacity. Reversible: revert Dockerfile.backend. |
| D78 | 2026-04-16 | **Position.status filters use `PositionStatus.OPEN` enum, never `"open"` string** — Postgres `positionstatus` enum is uppercase (`OPEN`, `CLOSED`, `EXPIRED`); literal `"open"` raises `psycopg2.errors.InvalidTextRepresentation` and crashed the exit_cascade nightly step. Regression test in `backend/tests/test_position_status_filter.py`. |
| D79 | 2026-04-16 | **Dashboard cache warming every 15 minutes** — `/market-data/dashboard` reads exclusively from Redis (cache miss returns 202). Without periodic warming the cache goes cold between nightly runs and after worker restarts, forcing every dashboard load to wait 30+s. Three Beat entries (universes `all`/`etf`/`holdings`) staggered at minutes 0/5/10 of every 15-min window keep all keys hot and absorb worker recycles cleanly. |
| D80 | 2026-04-16 | **Stale-session UX: explicit `/login` redirect on `auth:logout`** — When `/auth/refresh` itself 401s (expired refresh cookie or rotated token family), the API interceptor dispatches `auth:logout`. AuthContext clears React state but does not navigate. Added `AuthLogoutListener` inside `<Router>` to navigate to `/login` with a "Session expired" toast, eliminating the silent "Loading…" state on protected pages after long idle. |
| D105 | 2026-04-18 | **Refresh-token grace window + cross-tab single-flight.** New `users.previous_refresh_token_family` + `previous_refresh_token_rotated_at` plus a 10-second `REFRESH_TOKEN_GRACE_SECONDS` window let a concurrent tab present the immediately-prior refresh family without triggering a 401 cascade; the backend re-issues against the current family without rotating again. Frontend coordinates refresh across tabs via `authRefreshCoordination.ts` (localStorage lock + `BroadcastChannel('axiomfolio-auth')` with `storage`-event fallback) so only one tab calls `/auth/refresh`; the backend grace window is the backstop. Replay outside the grace window is still rejected. PR #345. |
| D112 | 2026-04-19 | **Concurrent-branch migration coordination protocol.** When multiple feature branches each create the same `0036_*.py`, the merge protocol is: (a) merge the canonical migration to `main` first; (b) re-merge `main` into each downstream branch and renumber its own migration to chain after the new latest revision; (c) re-run CI per branch before merging. Emerged organically during the v1 sprint as PRs #345 / #346 / #350 / #351 each authored a different `0036_*.py`. Now a documented runbook step. |
| D120 | 2026-04-20 | **Silent deploy-pipeline failures are P0. Deploy health telemetry must surface ≥2 consecutive `build_failed` events inside 10 minutes.** Surfaced live 2026-04-20: every Render deploy from 19:48 UTC through 03:10 UTC the next day (≥7 commits including PR #383 G22, PR #376 Trade Explainer, PR #353 dependabot, PR #382 docs, PR #384 docs cleanup, plus OAuth + multi-tenant foundation) was `build_failed` with `finishedAt - createdAt ≈ 1–2s`, i.e. Render rejected the build at queue time (billing / build minutes / account state — NOT a code defect). CI was green on every one of those PRs; `main` was green; PR pages showed merged and deployed. Prod stayed on `261349ad` for 8+ hours while every subsequent merge silently piled up. The `production-verification.mdc` rule ("CI green ≠ shipped; verify Render deploy status") existed but was not operationally enforceable because no alarm fires when a deploy fails to start. **Architectural commitment**: (1) a Beat job polls the Render API every 5 minutes for the latest deploy per service; if the last two deploys are both `build_failed` OR the latest successful deploy is >60 minutes stale while unmerged-to-deployed commit count ≥ 1, emit a P0 alert to `alerts_health` and surface on `/admin/health` as a red "Prod deploys stuck" row naming the stuck services and the last live commit SHA. (2) A `gh` pre-merge check — `gh pr checks` alone is insufficient; the merge workflow must additionally assert that the last deploy on `main` for both API and worker is `live`, else warn the merger that prod is already behind and their PR will compound it. (3) The `last_successful_prod_sha` is surfaced in every handoff doc and every `KNOWLEDGE.md` entry that claims "shipped". **Anti-goal**: never use "CI was green" or "the PR was merged" as evidence of production state ever again — those are necessary but not sufficient. Reversible: remove the Beat job and the pre-merge assertion if Render ever ships native deploy-failure notifications that are useful. Companion: G28 in `docs/plans/GAPS_2026Q2.md`; `production-verification.mdc`. |
| D121 | 2026-04-20 | **Evidence-based diagnosis: LLM-generated UI/config claims require source-document verification before they become handoff facts.** Surfaced live 2026-04-20: told the founder their IBKR Flex Query was "missing required sections (NAV in Base, Cash Report, etc.)" based on an LLM inference from a PARTIAL sync signal, without reading the founder's actual Flex Query config. Founder pushed back with a screenshot showing the query had every section G22's validator actually requires (plus all three optional ones plus bonus sections). The real cause of PARTIAL was Finding-2-adjacent: PR #383 had not actually deployed to production (D120), so the sync that returned PARTIAL-looking data was running on pre-G22 code that couldn't have set PARTIAL at all — it was the old "silent-empty-success" bug. The diagnosis cost the founder a write-up of the wrong fix. **Architectural commitment** (process, but binding): (a) before writing any claim of the form "your config X is missing Y" into a handoff doc, runbook, or user-facing message, the agent must either (i) read the actual config (e.g. screenshot, exported XML, API response), or (ii) explicitly scope the claim as a hypothesis ("candidate causes for PARTIAL are…") not a fact. (b) When G22 (or any validator) says PARTIAL/FAIL, the UI + log + error-message must include the exact missing-field names from the validator's own report, not a generic message — so agents and humans never need to infer. (c) Handoff docs must separate verified facts (with source: screenshot / query / log timestamp) from hypotheses / next-action-candidates. **Anti-goal**: never let "the LLM confidently said so" become a link in the evidence chain. Reversible only by re-training this habit; no code revert. Companion: G28 (UI surfaces exact missing-section names); `no-silent-fallback.mdc`; `no-hallucinated-ui-labels.mdc` (which this extends from UI labels to diagnostic claims). |
| D126 | 2026-04-21 | **G24 account-type-aware strategy shipped with read-only RiskGate hook.** Added `get_strategy_profile(account)` and wired account strategy context into rule-evaluator/exit-planner paths. `risk_gate.py` receives exactly one feature-flagged read-only invocation (`ENABLE_ACCOUNT_AWARE_RISK=False` by default) that does not mutate sizing/cap logic. Founder approved this touch on 2026-04-21. |
| D127 | 2026-04-22 | **Medallion architecture naming (bronze / silver / gold) is the layout contract for `backend/services/`.** Bronze = raw broker and market ingestion; silver = enrichment, indicators, regime, stage, portfolio analytics on top of ingested data; gold = strategies, candidates, picks, and other app-facing signal flows. New modules are created under `backend/services/<layer>/` from day one. Existing trees (e.g. `portfolio/`, `market/`, `picks/`) are grandfathered with module-level docstring tags until a deliberate move PR. See `docs/ARCHITECTURE.md` (Medallion Architecture). Reversible: convention and tags; physical moves are independent follow-ups. |
| D128 | 2026-04-21 | **Alpaca scaffolding dropped; autotrading runs through OrderManager -> RiskGate -> BrokerRouter against IBKR / Schwab / TastyTrade.** Alpaca was half-scaffolded (`alpaca_sync_service.py`, `alpaca_executor.py`, `alpaca_adapter.py`, `price_feed.py` Alpaca WebSocket) but never connected in production — no user ever authenticated, no rows landed. The scaffolding contradicted marketing (D129 broker truth) and misled the founder that Alpaca was needed for autotrading. Removal: deleted the four Alpaca-only modules, removed `ALPACA` from `BrokerType` enums in `broker_account.py` / `broker_oauth_connection.py` / `order.py`, dropped `alpaca-py` from requirements, removed Alpaca from frontend broker selectors, migration 0056 DELETEs any `broker='alpaca'` rows defensively and drops `'alpaca'` from the `broker_oauth_connections` CHECK constraint (replaced with `tradier` + `coinbase` ahead of their OAuth row usage). PG enum `brokertype` keeps `'alpaca'` as an unused value because PG enum value removal is non-trivial and the safety DELETE makes it harmless. Reversible: revert the PR; migration 0056 has a clean `downgrade()`. |
| D129 | 2026-04-21 | **Direct-OAuth-only broker expansion policy (v1).** Expanding broker support via direct OAuth integrations only: IBKR + Schwab + TastyTrade today; in-flight: E*TRADE (OAuth 1.0a) + Tradier (OAuth 2.0) + Coinbase (OAuth 2.0, crypto) per product sequencing in `docs/plans/`. No Plaid Investments aggregator in v1 — Plaid is positions-only fidelity (no options, trades, tax lots), which fails the quality bar that `MarketSnapshot` + Tax Center + options workflows require. `BROKERS_SUPPORTED` in `backend/constants/public_stats.py` must reflect the real direct-OAuth count; marketing pages read from that constant to prevent drift. Re-evaluate Plaid for brokers without OAuth (Fidelity, Vanguard, M1) if demand appears. Reversible: add a fourth bronze adapter later. |
| D130 | 2026-04-21 | **Bronze layer lives under `backend/services/bronze/<broker>/` and reuses OAuth adapters as signing primitives.** E*TRADE is the first broker ingested as a "medallion bronze" sync (raw broker JSON → canonical `Position` / `Option` / `Transaction` / `Trade` / `Dividend` / `AccountBalance` rows). Package layout: `backend/services/bronze/etrade/client.py` (thin data-API wrapper over `/v1/accounts/list`, `/balance`, `/portfolio`, `/transactions`, always `.json` suffix) + `sync_service.py` (shape mirrors `SchwabSyncService`: `sync_account_comprehensive` entry point, caller owns `Session` + commit, per-row loops emit `written/skipped/errors` counters with `assert sum == total`). HMAC-SHA1 signing is NOT duplicated — the client calls `ETradeSandboxAdapter._signed_request` (documented sanctioned `_protected` call, second caller); a change to signing touches one place. Credentials are read from `BrokerOAuthConnection` filtered by `(user_id, broker in ('etrade','etrade_sandbox'))` with both tokens decrypted via `backend.services.oauth.encryption.decrypt`; for OAuth 1.0a, `refresh_token_encrypted` holds the `access_token_secret`. Additional brokers (e.g. Fidelity) follow the same pattern. Celery: `backend.tasks.portfolio.etrade_sync.sync_all_etrade_accounts` fans out to `backend.tasks.account_sync.sync_account_task` on the `account_sync` queue, `time_limit=960s` matches `JobTemplate.timeout_s` (iron law). Cross-tenant isolation pinned by `backend/tests/services/bronze/etrade/test_sync_service_isolation.py`. Reversible: delete the package + migration 0060 (additive enum value only) + Beat entry; no destructive schema changes. |
| D131 | 2026-04-21 | **"Direct connections" is the accurate umbrella term; "direct OAuth" is not true for IBKR.** Copilot correctly flagged (PR #393 review) that the constant `DIRECT_OAUTH_BROKERS_LIVE` and the marketing copy in `WhyFree`, `PricingFAQ`, and `tier_catalog` all claimed "direct OAuth to Schwab, IBKR, and Tastytrade" — but IBKR is integrated via FlexQuery + IB Gateway, not OAuth. Schwab and TastyTrade are OAuth. Mixing them under "direct OAuth" was inaccurate marketing. Rename: `DIRECT_OAUTH_BROKERS_LIVE` → `DIRECT_CONNECT_BROKERS_LIVE`; the old name is kept as a deprecated alias (no breaking import). Copy rewritten to "direct connections to Schwab (OAuth), IBKR (FlexQuery + Gateway), and Tastytrade (OAuth)" where the distinction is relevant, and "direct broker integrations" where the umbrella is fine. `DIRECT_OAUTH_BROKERS_PLANNED` stays as-is for brokers not yet in `DIRECT_CONNECT_BROKERS_LIVE` (e.g. Coinbase) until promoted. Reversible: string-only change; no schema or API break. Companion: `no-hallucinated-ui-labels.mdc` — applies to marketing copy too, not just UI labels. |
| D132 | 2026-04-21 | **Tradier OAuth 2.0 bronze adapter.** Extends the D130 bronze contract to Tradier with both live (`tradier`) and sandbox (`tradier_sandbox`) OAuth **connection** slugs under `TradierOAuth2Adapter` + `TradierSandboxOAuth2Adapter` (same auth endpoint, different token/data hosts). `BrokerAccount` uses `TRADIER` / `TRADIER_SANDBOX` `brokertype` values (migrations 0061 / 0062) so `TradierSyncService` filters `BrokerOAuthConnection` by the matching slug; history/trade `external_id`/`execution_id` uses a short `tra_`+blake2b digest (≤50 chars for `Trade.execution_id`). Package: `backend/services/oauth/tradier.py` + `backend/services/bronze/tradier/{__init__,client,sync_service}.py`. Migrations: 0061 adds `TRADIER` to `brokertype` and recreates `ck_broker_oauth_connections_broker` to allow `tradier_sandbox` on `broker_oauth_connections.broker` (0056 had only `tradier`). 0062 adds `TRADIER_SANDBOX` to `brokertype`. See also D130 for the shared bronze/Beat pattern. Reversible: remove package + 0061/0062 + Beat entry; `brokertype` value removal is the usual PG limitation. |

### Platform Integration (Brain)

| ID | Date | Decision |
|----|------|----------|
| D21 | 2026-03-24 | **AxiomFolio is a Paperwork Brain skill** — exposes clean API surface |
| D32 | 2026-03-27 | **Brain API key auth** — `X-Brain-Api-Key` header validated via `secrets.compare_digest` |
| D33 | 2026-03-27 | **Unified notifications via Brain** — Discord removed, all alerts route through Brain webhook |
| D34 | 2026-03-27 | **Three-tier user roles** — owner/analyst/viewer replacing admin/user/readonly |
| D35 | 2026-03-27 | **Trade approval workflow** — Tier 3 actions require owner approval, ApprovalService tracks state |
| D43 | 2026-03-30 | **HMAC-SHA256 webhook signing** — body signing replaces plain secret; approve/reject bind to service identity |
| D69 | 2026-03-31 | **HTTP schedule tools** — `GET /tools/schedules` + `POST /tools/run-task` exposed to Brain (total Brain HTTP tools: 12) |
| D70 | 2026-04-06 | **Brain API key / webhook concentrated risk** — `BRAIN_API_KEY` and Brain webhook URL are a single M2M lane for outbound alerts. If the key is compromised or Brain is unavailable, **all** alerting on that path stops until rotated or restored. Mitigation: treat key like a production secret (rotation, least privilege), monitor delivery failures, and consider a **fallback alert channel** for critical ops signals. |
| D71 | 2026-03-31 | **Webhook events wired** — scan_alert (new Breakout Elite/Standard), regime_change (R-state shift), exit_alert (cascade non-HOLD) emit to Brain |
| D72 | 2026-03-31 | **Approval timeout** — stale PENDING_APPROVAL orders auto-rejected after 30 min; `sweep_stale_approvals` task every 5 min; `approval_expired` webhook |

### Risk & Execution

| ID | Date | Decision |
|----|------|----------|
| D36 | 2026-03-27 | **Circuit breaker with 3 tiers** — 2%/3%/5% daily loss limits, kill switch at 5% |
| D37 | 2026-03-27 | **Trading day resets at 4 AM ET** — configurable via trading_day_timezone/trading_day_reset_hour |
| D38 | 2026-03-27 | **TradingView webhook secrets hashed** — SHA-256 hex, constant-time comparison |
| D39 | 2026-03-27 | **Webhook orders start as PREVIEW** — OrderManager.submit() handles state transitions |

### V1 Greenfield (Subscription Platform)

Implementation decisions that shipped in PRs #326–#334 were previously logged under **D81–D88**, which duplicated the Strategic Direction **D81–D88** IDs (master-plan narrative vs. code-level decisions). They are renumbered **D95–D102** below. Older sprint notes or chat logs may still cite “D81–D88” for this block; they map to **D95–D102** here (`docs/TASKS.md` uses D95–D102).

| ID | Date | Decision |
|----|------|----------|
| D95 | 2026-04-09 | **Entitlement model is the single source of truth for feature access** (PR #326) — `entitlements` table keyed on `(user_id, feature)` with TierGate dependency injecting access checks at the route layer. FeatureCatalog enumerates every gated feature; tier mapping lives in code, not the DB, so changing a tier never requires a migration. Reversible. |
| D96 | 2026-04-09 | **Stripe webhook processor decoupled from EntitlementService via SubscriptionStateSink Protocol** (PR #330) — Lets the webhook handler ship before the persistence layer is finalized; tests exercise the processor with a fake sink. The HTTP 402 response on signature failure (vs 401) signals "billing problem" distinctly to Stripe's retry loop. Reversible. |
| D97 | 2026-04-09 | **Polymorphic email parser uses Preprocessor → LLMParser pipeline with pluggable provider** (PR #331) — Strips RFC822/HTML/PDF noise into a NormalizedEmail before the LLM ever sees it; LLM output is JSON-schema validated and per-record errors skip only the bad record. Provider is a Protocol; StubLLMParseProvider drives all tests. Reversible. |
| D98 | 2026-04-09 | **AgentBrain (PortfolioChat) is distinct from AutoOps AgentBrain** (PR #329, #333) — PortfolioChat answers user questions about their own holdings; AutoOps explains operator anomalies. Same architecture (provider abstraction + tool/runbook grounding) but different prompts, audit trails, and risk profiles. They will share an LLMProvider Protocol but never share state. |
| D99 | 2026-04-09 | **FileFree.ai tax-export schema is versioned and machine-readable** (PR #332) — `schema_version="1.0.0"` on every package; Pydantic `extra="forbid"` on lots/accounts/summary so silent drift fails loud; CSV column order is part of the contract. Bumping the version requires a KNOWLEDGE entry and a corresponding test update. |
| D100 | 2026-04-09 | **Tax export filters out tax-advantaged accounts by default** (PR #332) — IRA / Roth / HSA accounts skipped unless `include_tax_advantaged=true` is explicit. Prevents accidental inclusion of non-taxable activity in a 1099-shaped export. Reversible per-call. |
| D101 | 2026-04-09 | **AnomalyExplainer never raises in normal operation** (PR #333) — Every failure mode (provider error, malformed JSON, missing field, hallucinated task slug, > 12 steps) degrades to a per-category deterministic fallback runbook with `is_fallback=true` and `confidence=0.30`. AutoOps must never block because OpenAI is having a day. |
| D102 | 2026-04-09 | **OpenAIChatProvider is sync (`requests`), not async** (PR #334) — AutoOps callers (Celery beat tasks, admin routes) are sync; spinning up an event loop just to call one POST is wrong. The async `AgentBrain._call_llm` stays for the chat/tool loop; this is a sibling for one-shot operator calls. JSON-object response_format is forced upstream so the explainer's schema gate is the second line of defence, not the first. |
| D106 | 2026-04-19 | **Postmark inbound picks webhook** (PR #346) — `POST /api/v1/webhooks/picks/inbound` validates HMAC-SHA256 (Base64 digest with hex fallback), enforces a `PICKS_INBOUND_ALLOWLIST` (empty list rejects every request with `ignored/allowlist_empty`), and inserts into `EmailInbox` idempotently on the Postmark `MessageID` via `try/except IntegrityError` before dispatching the `parse_inbound_email` Celery task. `PICKS_INBOUND_REQUIRE_SIGNATURE=false` is a dev-only escape hatch; production requires `POSTMARK_INBOUND_SECRET`. |
| D110 | 2026-04-19 | **Daily AI portfolio narrative mirrors AnomalyExplainer architecture** (PR #350) — `PortfolioNarrative` model + `0036_add_portfolio_narratives` migration with a `NarrativeProvider` Protocol (OpenAI `gpt-4o-mini` / `StubNarrativeProvider` / `FallbackTemplateProvider`) so any provider failure degrades to a deterministic template instead of blocking the user surface. `fanout_daily_narratives` is a Celery beat job at `0 21 * * 1-5` America/New_York that generates per-user narratives; `persist_narrative` short-circuits within a 1-hour window. Frontend `DailyNarrative` card renders Markdown with skeleton/empty/error states and a "Why?" tooltip listing data sources. |
| D111 | 2026-04-19 | **Picks validator queue with state machine + tier-gated feed** (PR #351) — `CandidateQueueState` replaces `CandidateStatus` with the explicit DRAFT → APPROVED → PUBLISHED flow (REJECTED terminal); `Candidate` gains audit columns (`state_transitioned_at/by`, `source_email_parse_id`, `suggested_target/stop`, `published_at`) and a new `picks_audit_log` table records every transition. Subscriber `GET /api/v1/picks/published` is gated by the new `picks.feed_full` (Lite+) feature; Free users get a deduped preview grouped by `generator_name` via `row_number()` per source. |

### Data Pipeline

| ID | Date | Decision |
|----|------|----------|
| D40 | 2026-03-27 | **Redis Streams for real-time events** — signals:evaluated, signals:output (the prior price:feed:alpaca stream was dropped with D128). |
| D41 | 2026-03-27 | **Async Redis in async contexts** — redis.asyncio for FastAPI routes and services |
| D64 | 2026-04-02 | **iShares IWM ETF fallback for Russell 2000** — FMP/Finnhub lack R2K endpoint; parse CSV holdings from iShares; never cache empty constituent lists |
| D65 | 2026-04-02 | **HISTORY_TARGET_YEARS = 10** — one-time deep backfill, then delta-only nightly pipeline; 400-bar window sufficient for all rolling indicators |
| D66 | 2026-04-02 | **Agent chat tool_choice = auto** — first turn no longer forces tool call; out-of-scope guard in SYSTEM_PROMPT |
| D67 | 2026-04-02 | **User FK cascade policy** — CASCADE for user-owned data, SET NULL for audit/tracking columns; Alembic migration for 20+ FKs |
| D68 | 2026-04-02 | **Curated ETFs for multi-asset exposure** — IBIT, BITO, GLD, SLV, UNG, TLT, IEF, HYG, ARKK added to CURATED_MARKET_SYMBOLS |
| D73 | 2026-04-10 | **Materialized Views for dashboard aggregations** — Pre-compute breadth (% above SMA50/200), stage distribution, and sector performance into PostgreSQL materialized views (mv_breadth_daily, mv_stage_distribution, mv_sector_performance). Refreshed nightly via CONCURRENTLY after indicator computation. Eliminates 7M-row aggregation queries from API hot path. Alternatives: query-time aggregation (current, causes 502s), summary tables (more maintenance). Reversible: DROP VIEW. |
| D103 | 2026-04-18 | **Heatmap curated universe extended with thematic ETFs** — Added SMH, GDX, TAN, URA to `CURATED_MARKET_SYMBOLS` in `backend/services/market/constants.py` so `HeatmapView`'s `SECTOR_ETFS` rows (semis / gold miners / solar / uranium) are no longer filtered out when snapshots are scoped to the tracked universe. KWEB and LIT are not referenced by the current `SECTOR_ETFS` list and remain out of scope. PR #343. |

---

## Decision Details

### D134 [2026-04-21] Per-account risk profile as an additive layer

**Decision:** Introduce `BrokerAccountRiskProfile` — a per-account, user-editable set of risk caps — as a strictly additive layer on top of the firm-level caps in `backend/config.py` and the always-on enforcement inside `backend/services/execution/risk_gate.py`. The effective limit surfaced to any caller is `min(firm_cap, per_account_cap)` (see `backend/services/risk/account_risk_profile.py`). Attempts to persist an override that is looser than the firm cap are rejected with HTTP 400; values stricter than or equal to the firm cap are honoured. `RiskGate` itself is not modified by this change — it remains the single enforcement path (D6, D16, D53).

**Rationale:** The founder wants per-account dial settings (e.g. tighter caps on a taxable joint account vs an IRA) without risking accidental loosening of the firm-level discipline that protects capital (D2 regime gate, D108 command palette not relevant here). `min()` composition is the simplest invariant that preserves the "firm is always the ceiling" property no matter how users edit their row, and the service logs a WARNING whenever a persisted override exceeds the firm cap so a downstream observer can still detect degradation (see `.cursor/rules/no-silent-fallback.mdc`). All percent math is `Decimal` to match the Iron Laws.

**Alternatives considered:** (1) Add per-account caps directly into `RiskGate` — rejected: `risk_gate.py` is a Danger Zone and changing its inputs expands its blast radius; an additive configuration layer is cheaper and reversible. (2) Replace firm caps with per-account caps entirely — rejected: users would be able to loosen the system-wide discipline, which is the exact outcome this feature must prevent. (3) Allow the UI to write any value but clamp at render time — rejected: persisting a "wish" value that the system silently ignores is a footgun and violates the no-silent-fallback rule.

**Reversible?** Yes. The table is additive (migration 0068 creates it; `downgrade()` drops it). No existing rows or code paths depend on the new table being present. Callers of `RiskGate` still read the firm caps from `config.py`; the per-account service is opt-in.

### D127 [2026-04-22] Medallion architecture naming commitment

**Decision:** Formalize bronze/silver/gold layering for `backend/services/`. Bronze = raw ingestion; silver = enrichment + indicators + regime and related portfolio math; gold = analytics / signals / strategies / picks and similar app-facing flows. New modules go in `backend/services/<layer>/` from creation. Existing modules are grandfathered via module-level docstring tags until a dedicated relocation pass.

**Rationale:** A single vocabulary for raw vs analyzed vs product-facing code reduces wrong-way dependencies, matches how the data pipeline is already described (D130, market providers), and lets new work land in the right place without a risky whole-repo move in one step.

**Alternatives considered:** (1) Physically move all existing files into `silver/` and `gold/` in this change set — rejected: massive import churn and merge cost for the same logical contract. (2) Feature-only package layout with no layer names — rejected: does not make ingestion vs enrichment vs product surfaces obvious in code review.

**Reversible?** Partially. The naming convention is policy and can be revised; docstring tags are cheap to add or remove. Directory moves remain separate, reversible diffs.

### D44–D45, D69, D71–D72 — Paperwork Integration Sprint (2026-03-31)

**Celery Beat (D44)**: All scheduling driven by `backend/tasks/job_catalog.py` via Celery Beat. 20 catalog entries with cron, timezone, timeout. Render worker runs `celery ... worker --beat`. Three legacy Render cron jobs (`admin_coverage_backfill`, `admin_retention_enforce`, `ibkr-daily-flex-sync`) suspended. `render_sync_service.py` remains for optional non-Beat / Render-cron deployments.

**run_task_now routing (D45)**: `run_task_now` is MODERATE risk but implemented inline in `_tool_run_task_now`. Without `INLINE_ONLY_AGENT_TOOLS` membership, it falls to `_dispatch_celery_task` which has no mapping. Fix: add to INLINE_ONLY.

**HTTP schedule tools (D69)**: `GET /api/v1/tools/schedules` lists all catalog tasks with last run status. `POST /api/v1/tools/run-task` dispatches any catalog task immediately by ID. Total Brain HTTP tools: 12.

**Webhook events (D71)**: Three events wired to Celery tasks: `scan_alert` from `_run_scan_overlay` (compares previous vs new tier), `regime_change` from `check_regime_alerts` (regime_shift type), `exit_alert` from `evaluate_exits_task` (non-HOLD recommendations). All use `brain_webhook.notify_sync()`.

**Approval timeout (D72)**: Orders in PENDING_APPROVAL beyond `APPROVAL_TIMEOUT_MINUTES` (default 30) are auto-rejected by `sweep_stale_approvals` task (every 5 min). Fires `approval_expired` webhook. `GET /api/v1/tools/pending-approvals` lets Brain poll pending orders.

### D43 — HMAC-SHA256 Webhook Signing (2026-03-30)

Replace plain `X-Webhook-Secret` header with HMAC-SHA256 body signing. Remove `approver_user_id`/`rejector_user_id` from approve/reject endpoints — routes bind to `BRAIN_TOOLS_USER_ID`.

`webhook_client.py`: Serializes JSON, computes `hmac.new(secret, body, sha256)`, sends `X-Webhook-Signature: sha256=<hex>`.

Alternatives: Plain shared secret (rejected — no payload integrity), OAuth/JWT (overkill for M2M webhook). Reversible.

### D32–D41 — Gold Standard & Brain Integration (2026-03-27)

**Circuit Breaker (D36)**: Three daily loss tiers (2%, 3%, 5%) with progressive restrictions. Kill switch at 5% halts all trading.

**Brain Integration (D32, D33, D35)**: AxiomFolio exposes `/api/v1/tools/*` endpoints for Brain orchestrator. API key in header, webhooks for events. Outbound alerts route through the **Brain webhook** (D33), not Discord. Total Brain HTTP tools: 12.

**User Roles (D34)**: Migrated from `admin/user/readonly` to `owner/analyst/viewer`. Analyst can propose trades but requires owner approval.

**Async Redis (D41)**: All async functions must use `redis.asyncio` to avoid blocking the event loop. Sync Redis retained for Celery tasks.

### D31 — Agent Codebase Access Policy (2026-03-26)

Allowed: `services/`, `api/`, `tasks/`, `models/`, `utils/`, `tests/`. Denied: `.env`, `.pem`, `.key`, `secret`, `credential`, `password`. INLINE_ONLY tools always execute regardless of autonomy level.

### D28 — Greenfield DB Rebuild (2026-03-24)

Deleted 33 Alembic migrations, created single `0001_baseline.py`. Production was broken by blocked migration chain. Bundled: Google OAuth, refresh tokens, BrokerAdapter ABC, multi-tenant enforcement.

### D29 — Celery Task Routing (2026-03-25)

All task references must match `@shared_task(name="...")` exactly. Found 4 critical mismatches between `job_catalog.py` and actual task registrations.

### D3 — Scan Tier Rename (2026-03-28)

`Set 1` → `Breakout Elite`, `Set 2` → `Breakout Standard`, `Set 3` → `Early Base`, `Set 4` → `Speculative`, `Short Set 1` → `Breakdown Elite`, `Short Set 2` → `Breakdown Standard`. Names describe setup quality instead of internal jargon.

### D2 — Regime as Hard Gate (2026-03-23)

6 daily inputs → composite score → R1-R5: VIX spot, VIX3M/VIX, VVIX/VIX, NH-NL ratio, % above 200D, % above 50D. All position sizing, scan access, entry timing, and exit acceleration inherit current Regime.

---

## 10-Year Backfill Runbook

### Why 10 Years

- SMA150 + SMA200 need ~200 trading days of warmup; 5 years would give only ~3.5 years of usable stage history
- 10 years (2516 trading days) provides ~8 years of fully warmed-up data for backtesting
- RS Mansfield (252-day MA) needs 1 year of warmup — 10 years gives 9 years of RS data
- FMP historical data cost is flat (no per-year charge)

### Steps

1. **Increase `SNAPSHOT_DAILY_BARS_LIMIT`** in `backend/config.py` from 400 to 2600 (10 years + buffer)
2. **Run full backfill**: Use "Backfill Daily Coverage (Tracked)" from Operator Actions — covers all tracked symbols
3. **Recompute indicators**: "Recompute Indicators (Market Snapshot)" — recalculates all indicators with full 10-year series
4. **Verify**: Check Admin Health Dashboard — stage_quality should show <5% unknown rate, fundamentals should show >90% fill
5. **Russell 2000**: Add via "Sync Index Constituents" then run coverage backfill — this adds ~2000 symbols and will take longer

### Russell 2000 Coverage

- Index constituents sync supports `russell_2000` via FMP
- In prod, enable by adding to the tracked index list
- Backfill will take ~30-60 min for 2000 symbols at 10 years each
- FMP rate limits may require batching — the pipeline handles this with built-in retry/backoff

### Data Quality Checklist (Post-Backfill)

- [ ] Stage Quality: unknown_rate < 5%
- [ ] Fundamentals: fill_pct > 90%
- [ ] Daily coverage: > 98%
- [ ] RS Mansfield: non-null for all symbols with > 252 bars
- [ ] No UNKNOWN stages for symbols with > 175 bars of data

---

## Resolved Issues

| ID | Issue | Resolution |
|----|-------|------------|
| R1 | Celery 360s global limit kills long tasks | Raised defaults to 3500s/3600s, explicit per-task limits |
| R2 | Dual order paths will drift | OrderService merged into OrderManager |
| R3 | `get_portfolio_user` falls back to `User.first()` | Now requires JWT via `get_current_user` |
| R4 | RSI uses SMA not Wilder | False alarm — already uses Wilder smoothing |
| R5 | React Query v3 is legacy | Migrated to TanStack Query v5 |
| R6 | MarketDashboard.tsx 1400+ lines | Decomposed to 5-tab lazy-loaded views |
| R7 | Silent `except: pass` in indicators | Replaced with proper logging |
| R8 | Scan overlay field-name bugs | Fixed wrong column names |
| R9 | compute_daily_regime missing commit | Added session.commit() after persist_regime() |
| R10 | Intelligence tasks not registered | Added to celery_app.py include list |
| R11 | Circuit breaker banner on every page | Moved to PortfolioOverview only |
| R12 | Snapshot history 0% on dashboard | Unified query path in AdminHealthService with 5-min Redis cache |
| R13 | Render cron jobs retired | All scheduling via Celery Beat from job_catalog.py (D44) |
| R14 | Auto-ops backoff used 2^n exponentiation | Replaced with explicit BACKOFF_SEQUENCE tuple (15m, 30m, 60m, 2h) |
| R15 | run_task_now bypassed Celery dispatch | Added to INLINE_ONLY_AGENT_TOOLS — routes through _execute_safe_tool (D45) |
| R16 | ATR/ADX used SMA instead of Wilder's | Replaced with Wilder's exponential smoothing in indicator_engine.py (D46) |
| R17 | 52-week H/L used Close instead of High/Low | Fixed to use High/Low columns with Close fallback (D47) |
| R18 | Bollinger Bands used sample std (ddof=1) | Changed to population std (ddof=0) (D48) |
| R19 | TD Sequential could exceed 9-count | Capped at 9 with counter reset (D49) |
| R20 | UNKNOWN stage propagated through history | Skip UNKNOWN in run-length computation, display as "New" in UI (D51) |
| R21 | Regime rounding used banker's | Switched to half-up rounding (D52) |
| R22 | Exit cascade T2/T7 trailing stop structural bug | Fixed comparison to `current_price < hwm - trail_distance` |
| R23 | S4 short exit 35% tier unreachable | Reordered conditions: check 35% before 20% |
| R24 | OrderManager.submit skipped RiskGate | Added RiskGate.check before broker place_order (D53) |
| R25 | fundamentals_fill not scheduled | Added to job_catalog.py with daily 3:15 UTC cron |
| R26 | symbol String(10) truncation risk | Widened to String(20) via Alembic migration |
| R27 | PriceData.volume Integer overflow | Changed to BigInteger via Alembic migration |
| R28 | Zombie job runs stuck for 2h+ | Reduced STALE_JOB_RUN_MINUTES from 120 to 45 |
| R29 | scan_engine coerced None RS to 0 | Now skips Elite tiers when rs_mansfield is None |
| R30 | FMP capped at 250 calls/day on paid plans | Replaced per-var config with ProviderPolicy tiers; paid tier allows 100,000/day at 700 CPM (D54) |
| R31 | deep backfill short-circuited at L2 DB cache | Added skip_l2 param to fetch_daily_for_symbols; daily_since always bypasses L2 |
| R32 | Worker silently dead since Apr 10 — tz-aware Timestamp + forked engine pool | `provider_router.py` strips tz before exchange_calendars; `celery_app.py` `worker_process_init` calls `engine.dispose()` (commit a70f09a, 2026-04-15) |
| R33 | Async Redis client invalid across Celery event loops + DB pool exhaustion under concurrent backfills | `market_infra.py` re-initializes async Redis if loop is closed; `_daily_backfill_concurrency` capped at 20 to stay below SQLAlchemy pool of 30 (PR #314, 2026-04-15) |
| R34 | Worker OOM-killed daily on Render Starter (512 MiB) | Upgraded to Standard (2 GiB); `--max-memory-per-child` raised from 750000 to 1500000 KiB so Celery recycles before kernel OOM (D76, 2026-04-16) |
| R35 | Exit_cascade nightly step crashed: `invalid input value for enum positionstatus: "open"` | Replaced `Position.status == "open"` with `Position.status == PositionStatus.OPEN` in coverage.py and monitoring.py + regression test (D78, 2026-04-16) |
| R36 | Stale-session UX trap: page sat at "Loading…" forever after refresh-token expiry | `AuthLogoutListener` inside Router navigates to `/login` with toast on `auth:logout` event (D80, 2026-04-16) |
| R37 | API SIGSEGV (exit 139) ×6 — investigation closed | All 6 events occurred in one 5-hour window on 2026-04-10 during the deploy storm (7 PRs in one day). Zero SIGSEGVs since. Root cause: uvicorn graceful-shutdown race during rapid container churn (likely psycopg2/asyncio teardown). The pre-merge CI gate rule (commit 8a202e7, 2026-04-09) prevents the deploy-storm pattern. No code fix needed. |
| R38 | Snapshot history coverage 1.7% on most days (43/2534 symbols) | `recompute_universe` was silently swallowing per-symbol exceptions for 2491 symbols. Phase 0 fix adds structured counters (`written`, `skipped_no_data`, `errors`) to the loop in `backend/tasks/market/indicators.py` plus a final assertion that `written + skipped + errors == tracked_total`. Coverage test fails the build if assertion violated. Tracked under `fix/v1-phase-0-stabilization`. |
| R39 | 2820 monotonicity violations in `market_snapshot_history` + repair task killed by Postgres `statement_timeout` | `repair_stage_history_window` ran `SELECT DISTINCT symbol FROM market_snapshot_history` against a 7M+ row table with no covering index, exceeding the 30s statement timeout (D74). Phase 0 fix: Alembic migration `CREATE INDEX CONCURRENTLY ix_market_snapshot_history_symbol_btree`; rewrite query in `backend/services/market/stage_quality_service.py` to use `tracked_symbols_with_source` (already in cache). One-time bulk repair on the new heavy worker queue clears the 2820 violations. Tracked under `fix/v1-phase-0-stabilization`. |
| R40 | Distributed locks silently no-op on every account sync, order submit, TradingView webhook, and pre-trade risk check — `backend/services/cache` module didn't exist | Four call sites (`backend/tasks/portfolio/sync.py`, `backend/services/execution/order_manager.py` DANGER ZONE, `backend/api/routes/webhooks/tradingview.py`, `backend/services/risk/pre_trade_validator.py`) did `from backend.services.cache import redis_client` inside a broad `try/except Exception` block, so every lock acquisition raised `ModuleNotFoundError`, logged a warning, and proceeded **without** a lock. Prod worker logs showed thousands of `Redis lock failed for account X sync, proceeding: No module named 'backend.services.cache'`. Fix: created `backend/services/cache.py` exposing a lazy, memoized sync `redis_client` (via module `__getattr__` so unit tests don't need `REDIS_URL` at import time). Call-site try/except preserved so behavior on a broken Redis still fails-open with a warning; on a healthy Redis the lock now actually holds. Regression test `backend/tests/test_services_cache.py` asserts the module is importable and the client is memoized. 2026-04-21. |

---

## New Decisions

- **D122 (2026-04-21): Moat-aligned three-axis BYOK-aware pricing.** We price by MCP tool depth first (moat), native chat second (convenience), and BYOK availability third (margin lever) so LLM commodity spend is decoupled from product value.
- **D123 (2026-04-21): Ladder 3 supersedes D82.** `LITE` is removed and collapsed into `PRO`, with migration-side grandfather metadata for former Lite subscribers.
- **D124 (2026-04-21): D87 LLM-budget framing is superseded.** Entitlements are now anchored to tool depth and MCP scope, not an internal token-wallet budget narrative.
