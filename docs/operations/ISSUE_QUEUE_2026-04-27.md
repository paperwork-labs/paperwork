---
owner: agent-ops
last_reviewed: 2026-04-27
doc_kind: checklist
domain: strategy
status: active
---

# Open issue queue — `paperwork-labs/paperwork`

**Triage date:** 2026-04-27  
**Scope:** Open issues at sweep time, ordered for contributor/agent pickup. Labels: `ready` = well-specified; `blocked-on-founder` = needs credentials or product call before execution.

**Sweep result:** One open issue carried a `ready` label after triage; four YELLOW follow-up issues were **closed as resolved** because corrective work had already merged (see [PRs #248, #249, #250, #251](https://github.com/paperwork-labs/paperwork/pulls?q=is%3Apr+is%3Amerged+250+249+248+)). No issues met “stale” or `needs-info` rules among open items.

---

## Top 10 `ready` issues

Sorted by:

1. `blocked-on-founder` + `ready` (founder visibility)
2. `bug` + `ready`
3. `enhancement` (feature) + `ready`

*Fewer than 10 open issues were labeled `ready` at this run — list is exhaustive for that set.*

| # | Title | Why it matters | Suggested approach | Agent type |
|---|--------|----------------|-------------------|------------|
| **79** | [EPIC] Agent-Driven PR Automation — Brain side (Brain as Dev OS, Phase 1) | Delivers D17 tool tiers (draft PR, merge, doc updates) and Brain-driven PR loop; unblocks the wider agent automation roadmap. | Phase 1: HMAC-verified `POST /api/v1/webhooks/github`, Pydantic event models, persist to `brain_github_events` per epic; then Cursor BG dispatcher + write tools with tier gates and spend caps. | **generalPurpose** (cross-cutting Python services, secrets, idempotency); **shell** for local migration/tests; **ai-architect** if splitting phases; browser only for Slack/n8n validation. |

### Notes

- **Founder-action checklist:** infra consolidation was not on `main` at triage time (see open PRs under `feat/founder-actions-*`); founder-dependent steps inferred from issue (GitHub App, `GITHUB_WEBHOOK_SECRET`, Cursor Background Agents, Slack n8n).
- **Epic #79** is also labeled `epic`, `agent-automation`, and `infrastructure` and is assigned; sub-phases are suitable for small PRs per checklist.

---

*Generated by the 2026-04-27 triage sweep; re-run monthly or when backlog shifts materially.*
