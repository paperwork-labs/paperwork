/**
 * Legacy Chakra `createSystem` theme previously lived here. The app shell now uses Tailwind and
 * semantic tokens in `index.css` (see comments there for the token map).
 *
 * Do not reintroduce `@chakra-ui/react` at the app root; extend Tailwind/shadcn instead.
 */

export {};
