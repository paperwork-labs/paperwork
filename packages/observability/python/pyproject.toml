[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "observability-paperwork"
version = "0.1.0"
description = "Lightweight Paperwork error capture client"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["."]
