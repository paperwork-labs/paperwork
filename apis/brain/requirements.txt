# Web framework
fastapi>=0.136.1
uvicorn[standard]>=0.32.0
gunicorn>=23.0.0

# Database
sqlalchemy[asyncio]>=2.0.49
asyncpg>=0.30.0
alembic>=1.18.4
pgvector>=0.4.2

# Validation & settings
pydantic>=2.13.3
pydantic-settings>=2.6.0
python-multipart>=0.0.17

# Auth
python-jose[cryptography]>=3.3.0

# AI / LLM — multi-provider (D20)
anthropic>=0.97.0
openai>=1.57.0

# MCP server
fastmcp>=2.6.0

# Redis
redis>=7.4.0

# Observability (D41)
langfuse>=2.0.0

# Utilities
httpx>=0.28.1
slowapi>=0.1.9
cryptography>=44.0.0
pyyaml>=6.0.3
tiktoken>=0.11.0

# In-process scheduler for PR sweeps (Track B, Week 1). No external daemon.
# [sqlalchemy] enables SQLAlchemyJobStore; psycopg2 is the sync driver for the job store.
apscheduler[sqlalchemy]>=3.11.2
psycopg2-binary>=2.9.9

# Dev / Test
pytest>=9.0.3
pytest-asyncio>=1.3.0
ruff>=0.15.12
mypy>=1.13.0
types-PyYAML>=6.0.12  # mypy stubs for `import yaml` (sprint_planner.py); silences import-untyped under strict mypy
