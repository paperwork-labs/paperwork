"""Accessibility + design system pillar.

medallion: ops
"""

from __future__ import annotations


def collect() -> tuple[float, bool, str]:
    return (50.0, False, "bootstrap estimate — axe-core CI deferred")
