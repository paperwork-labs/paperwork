"""Web perf + UX pillar — Lighthouse-CI deferred.

medallion: ops
"""

from __future__ import annotations


def collect() -> tuple[float, bool, str]:
    return (55.0, False, "bootstrap estimate — Lighthouse-CI deferred")
