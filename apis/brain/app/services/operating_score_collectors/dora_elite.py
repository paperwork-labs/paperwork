"""DORA pillar — placeholder until GH / deploy telemetry collectors land.

medallion: ops
"""

from __future__ import annotations


def collect() -> tuple[float, bool, str]:
    return (75.0, False, "bootstrap estimate — DORA collector deferred to follow-on PR")
