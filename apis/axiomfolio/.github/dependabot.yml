version: 2
updates:
  # Frontend (npm)
  - package-ecosystem: "npm"
    directory: "/frontend"
    schedule:
      interval: "weekly"
      day: "sunday"
      time: "06:00"
    open-pull-requests-limit: 5
    labels:
      - "deps"
      - "frontend"
    ignore:
      # react-dom major bumps require react major; we stay on React 18 for now
      - dependency-name: "react-dom"
        update-types: ["version-update:semver-major"]
      # eslint 10 breaks @testing-library/react type resolution in tests; skip major for now
      - dependency-name: "eslint"
        update-types: ["version-update:semver-major"]
    groups:
      frontend-minor-and-patch:
        applies-to: version-updates
        update-types:
          - "minor"
          - "patch"
      frontend-devdeps:
        applies-to: version-updates
        dependency-type: "development"
        update-types:
          - "minor"
          - "patch"

  # Backend (pip) - requirements.txt at repo root
  - package-ecosystem: "pip"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "sunday"
      time: "06:00"
    open-pull-requests-limit: 5
    labels:
      - "deps"
      - "backend"
    groups:
      backend-minor-and-patch:
        applies-to: version-updates
        update-types:
          - "minor"
          - "patch"

  # GitHub Actions
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "sunday"
      time: "06:00"
    open-pull-requests-limit: 5
    labels:
      - "deps"
      - "ci"

