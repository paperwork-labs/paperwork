{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": null,
  "installCommand": "bash ../../scripts/vercel-install.sh @paperwork-labs/design",
  "buildCommand": "pnpm --filter @paperwork-labs/design build-storybook",
  "outputDirectory": "storybook-static",
  "git": {
    "deploymentEnabled": {
      "main": true
    }
  },
  "ignoreCommand": "bash -c 'if git log -1 --pretty=%an | grep -qi dependabot; then echo \"Dependabot commit, skipping build\" && exit 0; fi; npx -y turbo-ignore @paperwork-labs/design --fallback=HEAD~10'"
}
