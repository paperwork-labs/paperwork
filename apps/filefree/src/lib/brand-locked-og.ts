/**
 * OG / `next/og` ImageResponse — literals mirror `src/app/globals.css` (`:root` / `.dark`).
 * Edge runtime has no CSS variable cascade; keep in sync when the FileFree lock changes.
 */
export const filefreeBrandOg = {
  bgGradientStart: "#020817",
  bgGradientMid: "#0f172a",
  bgGradientEnd: "#1e1b4b",
  text: "#F8FAFC",
  textMuted: "#94a3b8",
  gradientFrom: "#4F46E5",
  gradientTo: "#84CC16",
} as const;
